const saldo = 1
const unidade = (saldo === 1 ? " real" : " reais");

console.log(saldo + unidade)




//console.log(`Seu saldo é de ${saldo} reais`);


//if (saldo === 1) { console.log(`Seu saldo é de ${saldo} real`) } else { console.log(`Seu saldo é de ${saldo} reais`); }

